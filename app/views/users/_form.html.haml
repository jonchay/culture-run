= form_for(@user) do |f|
  -if @user.errors.any?
    .error_explanation
      #{pluralize(@user.errors.count, 'error')} prohibited this event from being saved:
      %ul
        - @user.errors.full_messages.each do |msg|
          %li= msg
